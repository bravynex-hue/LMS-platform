# Install Socket.IO

Run these commands:

## Server
```bash
cd server
npm install socket.io
```

## Client
```bash
cd client
npm install socket.io-client
```

After installation, the WebSocket implementation will be ready!
